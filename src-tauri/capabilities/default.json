{
  "identifier": "default",
  "description": "Default capabilities for ProjectLoupe",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:default"
  ]
}

